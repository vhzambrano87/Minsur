@model WebAppMS.Models.DTOIngresonivelespozaM

@{
    ViewBag.Title = "Ingresonivelespoza";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header>
    <span class="widget-icon"> <i class="fa fa-edit"></i> </span>
    <h2>Modificar Ingreso Niveles Poza </h2>

</header>

<!-- widget div-->
<div>

    @if (Model.resultado == 1)
    {
        <div class="alert alert-block alert-info">
            <a class="close" data-dismiss="alert" href="#">×</a>
            <h4 class="alert-heading">Información</h4>
            <p>
                Se modificó correctamente la información.
            </p>
            <br>
        </div>
    }

    @if (Model.resultado == 2)
    {
        <div class="alert alert-block alert-info">
            <a class="close" data-dismiss="alert" href="#">×</a>
            <h4 class="alert-heading">Error</h4>
            <p>
                No se pudo modificar la información.
            </p>
            <br>
        </div>
    }

    <!-- widget edit box -->
    <div class="jarviswidget-editbox">
        <!-- This area used as dropdown edit box -->

    </div>
    <!-- end widget edit box -->
    <!-- widget content -->
    <div class="widget-body no-padding">

        <form id="order-form" class="smart-form" novalidate="novalidate" method="post">
            @Html.HiddenFor(model => model.ingresonivelespoza_id)
            @Html.HiddenFor(model => model.fecha_desc)
            @Html.HiddenFor(model => model.turno)
            <fieldset>
                <div class="row">
                    <section class="col col-6">
                        <label class="label">Fecha</label>
                        <label class="input">
                            @Html.HiddenFor(model => model.fecha)
                            <i class="icon-append fa fa-calendar"></i>
                            <input id="fecha_text" type="text" name="request" class="datepicker" data-dateformat='dd/mm/yy'>
                        </label>
                        <br />
                        <label class="label">Turno</label>
                        <label class="select">
                            @Html.DropDownList("ListaTurno123", (IEnumerable<SelectListItem>)ViewBag.ListaTurno, new { @class = "select" })
                        </label>
                        <br />
                        <label class="label">Cotapls</label>
                        <label class="input">
                            @Html.EditorFor(model => model.cotapls)
                            @Html.ValidationMessageFor(model => model.cotapls, "", new { @class = "text-danger" })
                        </label>
                        <br />
                        <label class="label">Volumepls</label>
                        <label class="input">
                            @Html.EditorFor(model => model.volumepls)
                            @Html.ValidationMessageFor(model => model.volumepls, "", new { @class = "text-danger" })
                        </label>
                        <br />
                        <label class="label">Cotapge</label>
                        <label class="input">
                            @Html.EditorFor(model => model.cotapge)
                            @Html.ValidationMessageFor(model => model.cotapge, "", new { @class = "text-danger" })
                        </label>
                        <br />
                        <label class="label">Volumepge</label>
                        <label class="input">
                            @Html.EditorFor(model => model.volumepge)
                            @Html.ValidationMessageFor(model => model.volumepge, "", new { @class = "text-danger" })
                        </label>
                        <br />
                        <label>
                            <i></i>Activo @Html.CheckBoxFor(model => model.activo)
                        </label>
                    </section>
                </div>
            </fieldset>

            <footer>
                <a href="@Url.Action("Index", "Ingresonivelespoza")" class="btn btn-primary">Volver</a>
                <input type="submit" class="btn btn-primary" name="submit" value="Modificar" onclick="ObtenerCombos()">
            </footer>
        </form>

    </div>
    <!-- end widget content -->
</div>
@section pagespecific {
    <script type="text/javascript">

            // DO NOT REMOVE : GLOBAL FUNCTIONS!

            $(document).ready(function () {
                $('body').css('zoom', '80%');
                document.getElementById("fecha_text").value = document.getElementById("fecha_desc").value;
            });
    </script>
}
<script type="text/javascript">
    function ObtenerCombos() {
        debugger;

        document.getElementById("turno").value = document.getElementById("ListaTurno123").value;
        document.getElementById("fecha_desc").value = document.getElementById("fecha_text").value;

    }

</script>